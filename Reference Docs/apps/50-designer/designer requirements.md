Requirements aren't here yet because this app works pretty well already, but this app injests the JSON from 40 and puts it into a presentation deck format that the user can edit and then save out.

IMPORTANT: At least 1 output from 40-reporter should be exactly the JSON expected by this app (see below)