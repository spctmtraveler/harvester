# Legacy Code (Reporter)

Reference-only.

- `harvester_forcefit_v24.html`
  - Primary: Reporter/Synthesizer hybrid (force-fit organization + export to JSON/Markdown)
  - Uses older bucket taxonomy; treat as UI/prompt inspiration, not current logic.
